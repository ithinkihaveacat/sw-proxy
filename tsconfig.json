{
  // Intended to be run via "npm run prepublish", which provides the
  // missing arguments.
  // https://www.typescriptlang.org/docs/handbook/compiler-options.html
  "compilerOptions": {
    "target": "es6",
    "strict": true,
    "sourceMap": false,
    "moduleResolution": "node",
    // Don't check anything in node_modules/@types:
    // https://www.typescriptlang.org/docs/handbook/tsconfig-json.html#types-typeroots-and-types
    "types": []
  },
  // TODO Fix examples/*.ts so this isn't necessary
  "files": [
    "proxy.ts",
    "router.ts"
  ]
}
